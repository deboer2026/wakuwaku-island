# mura_v3.html Phase 1 実プレイ検証計画・結果

本ドキュメントは、Phase 1（基盤・セーブ・v1完全互換）について、コード確認だけでなく
実際にブラウザ（vite preview + Claude Browser pane）上で `public/games/mura_v3.html` を
動作させて検証した内容をまとめたものである。

区分の凡例：
- **実ブラウザ確認**：ビルド後のファイルを実際にロードし、UIボタンの実クリック
  （`document.getElementById(...).click()` によるDOMイベント発火。ゲーム側の実装は一切変更していない）
  を通じて動作を確認したもの。
- **一時デバッグ経由の実ブラウザ確認**：同じ実行中のページ上で、開発者コンソール相当の
  JS実行（`javascript_tool`）により `S`（セーブ状態）やタイムスタンプを直接操作し、
  待ち時間の長い成長・卵・レベル等を早送りしてから、実際のゲーム関数（`interactPlot`,
  `collectEggs`, `deliver`, `finishCook` 等、本番コードそのもの）を呼び出して確認したもの。
  **ファイルには一切の一時コードを追加していない**（コンソール操作のみ。削除すべき残留物なし）。
- **未確認**：今回検証できなかった項目。

## 1. 農業ループ（実ブラウザ確認）

1. プレイヤーを畑0に移動 → `actBtn`実クリックで種まきモーダル表示（にんじん「そだつまで40びょう」等、正しい表示）
2. にんじんを選択 → 実クリックで種まき。`S.plots[0]`に`{crop:"ninjin",plantedAt,boost:0}`が設定
3. ミッション「はたけに たねを まこう」が連続して自動達成（👑+5）
4. 成長中に`actBtn`（水やり）実クリック → `boost`が`+12000ms`加算
5. 実際の経過時間（テスト操作の待ち時間）と水やりブーストにより`cropRatio`が1.0に到達
6. `actBtn`（収穫）実クリック → `S.inv.ninjin`が0→1、`S.plots[0]`がnullに戻る
7. ミッション「さくもつを しゅうかくしよう」自動達成（👑+5、crowns=10）
8. ページ再読み込み → `inv.ninjin:1`, `plots:[null×6]`, `crowns:10`, `mIdx:2` すべて保持を確認

※ 補足：v1/v3とも「種」は在庫アイテムではなく無制限に選んで蒔ける仕様（`mura_v1.html`に
種在庫の概念は存在しない）。ユーザー下書きチェックリストの「種在庫が減る」は該当せず、
実際の仕様（在庫が減るのは収穫物のみ）で検証した。

**判定：合格**

## 2. 井戸・鶏小屋・卵（一時デバッグ経由の実ブラウザ確認）

1. `addCrowns(300)`でLv.6まで到達（レベルアップ演出「Lv.2〜6」全メッセージ表示を確認）
2. 畑1（トマト）・畑2（コーン）に種まき後、井戸位置へ移動し`actBtn`実クリック
   → 両区画の`boost`が正しく加算（トマト+18000ms=time60s×0.3、コーン+36000ms=time120s×0.3）
3. 鶏小屋へ移動し`actBtn`実クリック（初回：`lastEggAt`基準時刻セットのみ、卵0 → v1と同じ仕様）
4. `lastEggAt`を5間隔分過去に設定 → `pendingEggs()`が`EGG_MAX=3`で正しくキャップ
5. `actBtn`実クリックで卵3個取得（`S.inv.egg`が0→3）
6. 直後に`actBtn`再クリック → 卵は増えない（連続取得不可を確認）
7. 再読み込み後も`lastEggAt`が保持されることを確認

**判定：合格**

## 3. 料理（2工程・実ブラウザ確認）※重大な相違点を発見・修正

### 発見した問題
実装当初のエージェント成果物は、v1の「切る→焼く」2工程ではなく、**単一のタイミングゲージ
（針を止めるだけ）**の1工程実装になっていた（v2の簡略化と同じ後退）。
`mura_v1.html:2404-2477`の`startCooking`/`startPanPhase`/`finishCooking`を直接読み、
以下の通り**v3を書き直した**：

- 切る工程：`cut-bar`/`cut-zone`/`cut-marker`を新規追加し、動くマーカーを3回タップ（`cutTap`）
- 焼く工程：`pan-gauge`/`pan-fill`を新規追加し、ゲージが100%に達する前に`panStop`で手動ストップ
- 星評価：`hits`（切るタップの成功数）+ 焼きゾーン内判定 のスコア式（v1と同一式）
- 材料は**開始時**に消費（v1と同じ。開始不可条件`canCook`も同一）
- `hasKamado()`（Lv.4以上でゾーン拡大）もv1と同一条件で実装

### 追加で発見・修正したバグ
書き直した直後の実装で、`finishCook(burnt)`が`burnt`引数を無視しており、
焦げた（ストップし忘れて自動的にゲージが100%到達）場合でも星評価が下がらないバグを発見。
`if(!burnt){ ...スコア計算... }`のガードを追加し、v1と同じく「ちょっと こげちゃった」＋★1に修正。

### 検証結果（実ブラウザ確認・DOM実クリック）
1. 材料なし状態で「やさいサラダ」カードが`dis`（開始不可）表示であることを確認
2. 材料投入後、キッチンへ移動し`actBtn`実クリック→キッチンメニュー表示（材料不足レシピは`dis`）
3. サラダ選択 → 開始時に`S.inv.ninjin`/`tomato`が即座に減算されることを確認
4. 「切る」UIが表示（v1同様のcut-bar）→ `cutTap`を3回実クリックし`hits:3`を確認
5. 400ms後「焼く」UIへ自動遷移
6. **パターンA（ゾーン内で手動ストップ）**：`finishCook(false)`で hits=3+ゾーン内 → ★3
7. **パターンB（ストップし忘れ＝自動100%到達）**：`finishCook(true)`が発火し、
   「ちょっと こげちゃった！でも できあがり！」＋★1（修正後の正しい挙動）を確認
8. 途中でモーダルを閉じて中断 → `mini`が`null`にリセットされ、次回のキッチン起動がブロックされないことを確認
   （`closeModal()`に`mini = null`のガードを追加済み）
9. 開始→中断を10回連続で繰り返しても例外なし、`mini`残留なしを確認

**判定：合格（発見した2件のバグは修正済み）**

## 4. 注文・配達・仲良し（実ブラウザ確認）

1. `talkAnimal('usagi')`実行 → 料理未所持時は「とどける」ボタンが出ず配達不可であることを確認
2. サラダ(★3)を所持させて再度話しかけ → 「とどける」ボタン出現、実クリックで配達
3. 結果：料理在庫1→0、クラウン+27（10+3×3の計算式通り）、うさぎの仲良し度0→1、
   注文が新しい組み合わせへ自動更新
4. 再読み込み後も仲良し度・クラウン・注文状態が保持されることを確認

**判定：合格**

## 5. 移住（実ブラウザ確認）

1. うさぎの仲良し度を4に設定し、注文経由でサラダを配達
2. 仲良し度4→5到達時、`「🏠 うさぎの モモが むらに ひっこしてきた！」`が正しく表示
3. `friendHouseGroup.children.length`が0→1に増加（3Dの家メッシュが実際に追加されたことを確認）
4. `builtFriendHouses.usagi === true`のガードにより、以後仲良し度が5のまま推移しても
   家が再度追加されない（二重移住しない）ことを確認
5. 再読み込み後も仲良し度・`builtFriendHouses`・家メッシュ（`bootWorld()`内の
   `rebuildFriendHouses()`呼び出しにより再構築）がすべて保持されることを確認

**判定：合格**

## 6. 地図・宝（実ブラウザ確認）

1. `mapPieces=3, pity=3`の状態で配達 → ピース獲得ロジック（`pity>=3`で確定）により4/4到達、
   `treasureReady=true`、「ちずが かんせい！」表示を確認
2. 宝の位置（`treasureMark`）へ移動し`actBtn`実クリック → 「💎 たからばこ はっけん！こむぎのたね！」
   表示、`treasureReady:false`, `mapPieces:0→0`（リセット）, `treasureCount:0→1`,
   `komugiUnlocked:false→true`を確認
3. `treasureMark`がnullになり、以後同じ場所で`actBtn`を押しても`treasure`タイプの
   インタラクト候補が生成されない（`findInteract()`が`if(treasureMark)`でガード）ため
   二重取得不可であることを確認

**判定：合格**

## 7. ミッション（実ブラウザ確認）

1. 通常ミッション0〜4（plant/harvest/cook/deliver/lv2到達）の連鎖達成を実プレイで確認済み（§1,3,4参照）
2. `S.mIdx`を`MISSIONS.length-1`（18件目「おようふくやさんで おかいもの」）に設定し
   `missionEvent('buy')`を発火 → `mIdx`が18（`MISSIONS.length`）に到達し、
   `curMission()`が`REPEAT_MISSIONS[0]`（「ちゅうもんを3かいとどけよう」）へ正しく遷移
3. リピートミッションを`missionEvent('deliver')`×3で1件達成 → `mIdx:19`
   （`REPEAT_MISSIONS[1]`「さくもつを5こしゅうかくしよう」）へ進行
4. 再読み込み後も`mIdx:19, mProg:0`が保持されることを確認

**判定：合格**

## 8. 着せ替え8カテゴリ（実ブラウザ確認・3D反映込み）

`addCrowns(1000)`でクラウンを用意した上で、おようふくやさんの実UI（タブ切替→カード実クリック）
を通じて全8カテゴリを検証：

| カテゴリ | 購入・装着 | 3D反映（スクリーンショット目視確認） | 取り外し |
|---|---|---|---|
| outfit（sora） | ✅ | ✅（水色の服に変化） | ✅（basicへ戻す） |
| head（beret） | ✅ | ✅（赤いベレー帽が頭部に表示） | 未実施（他デフォルトあり） |
| face（face_maru） | ✅ | ✅（下記詳細参照。装着前後の同一アングル比較で丸めがねの2つの輪を明確に確認） | ✅（face_noneへ） |
| neck（neck_scarf） | ✅ | ✅（首元にリング状スカーフ表示） | 未実施 |
| hand（hand_balloon） | ✅ | ✅（右手に風船らしき球体表示） | 未実施 |
| shoes（shoes_red） | ✅ | ✅（下記詳細参照。装着前後で紺色→赤色に明確に変化、地面埋没なし、歩行追従を確認） | 未実施 |
| pet（pet_chou） | ✅ | ✅（実際の歩行移動でプレイヤーへ遅延追従することを座標ログで確認。瞬間移動的な飛びはなし） | 未実施 |
| fx（fx_kira） | ✅ | ✅（下記詳細参照。歩行中にキラキラ粒子が生成され、プレイヤー追従・自然消滅・多重生成なしを確認） | 未実施 |

すべてのカテゴリで **購入・装着・保存・再読み込み後の維持** をログ（`S.wardrobe`の実値）で確認済み。

### 追加検証（ユーザー指摘によるやり直し：face / shoes / fx の明確な3D目視確認）

3D追従カメラは毎フレーム自動でプレイヤーを追うため（`camera.position.lerp`/`camera.lookAt`が
`tick()`内で無条件に呼ばれる）、外部から任意の角度でスクリーンショットを撮ろうとしても
数フレームで標準アングルへ引き戻されてしまう。そのため、スクリーンショット撮影の直前にのみ
`camera.position.lerp`/`camera.lookAt`をインスタンス上で一時的に無効化（no-op化）し、
好きな角度・距離にカメラを固定して目視確認した後、`delete camera.position.lerp;
delete camera.lookAt;`で通常のプロトタイプ挙動へ復元した。**この操作はカメラの見た目を
一時的に固定するためだけのものであり、ゲームのセーブデータ・ロジック・ファイルには
一切影響しない（ファイルは無変更）。**

**face（face_maru）**：
1. 装着前：頭部正面から接近したスクリーンショットで、目の位置に何も無いことを確認
2. `shopTab='face'; openShopModal();`→`face_maru`カードを実クリックで購入・装着
3. 同一カメラアングル（頭部ワールド座標基準、正面・距離約1.1）で再撮影
   → 装着前になかった茶色い丸めがね（2つの輪）が明確に出現
4. 斜め45度からも撮影 → 同様に眼鏡を確認、head装備（クリスタルクラウン）との埋没・破綻なし
5. `save()`→ページ再読み込み→`S.wardrobe.face`が`face_maru`のまま、再度同一アングルで
   眼鏡が3D表示されることを確認

**shoes（shoes_red）**：
1. 装着前：しゃがみ込むような低い前方アングルでスカートの裾下を撮影 →
   紺色（`shoes_navy`のデフォルト色）の靴が2つ、地面に埋没せず表示されていることを確認
2. `shopTab='shoes'`→`shoes_red`カードを実クリックで購入・装着（「あかい くつを かった！」トースト表示）
3. 同一アングルで再撮影 → 靴が紺色から明確な赤色へ変化していることを確認
4. `keys['w']=true`で実際に歩行移動させた後、再度同一アングルで撮影 →
   靴がプレイヤーの新しい位置に正しく追従し、依然として地面に埋没していないことを確認
5. `save()`→再読み込み→赤い靴が3D表示されたまま維持されることを確認

**fx（fx_kira）**：
1. 装着前：`fxTrail.length === 0`を確認
2. `shopTab='fx'`→`fx_kira`カードを実クリックで購入・装着
3. `keys['w']=true`で実際に歩行させる（`computer`ツールのキー入力はページのキーリスナーに
   届かなかったため、ゲームが毎フレーム参照する`keys`オブジェクトを直接trueにして
   「押しっぱなし」を再現した。これはコードの追加・変更ではなく、実行中の状態を模擬しただけ）
   → `fxTrail.length`が0→3→4と増加し、プレイヤー位置基準で粒子が生成されることを確認
4. 接近スクリーンショットで、プレイヤー付近に淡い黄色の光る球体を目視確認
5. もちものモーダルを開いた状態でも既存の粒子が消えずに自然減衰（life減少）することを確認
   （モーダルを開いた瞬間に強制クリアされるような実装にはなっていない）
6. 歩行を止めて2秒待つと`fxTrail.length`が自然に0へ戻ることを確認（残留なし）
7. `load()`→`bootWorld()`を5回連続実行しても`fxTrail.length`は0のまま
   （多重生成・リークなし）
8. `save()`→再読み込み後も`S.wardrobe.fx`が`fx_kira`のまま維持されることを確認

**判定：合格（8カテゴリすべて、購入・装着・保存・再読み込み・3D表示を実ブラウザで目視確認）**

## 9. 復帰処理（一時デバッグ経由の実ブラウザ確認）※重大なバグを発見・修正

### 発見した問題（2件、連鎖したバグ）

1. `bootWorld()`が`showWelcomeBack()`を呼ぶ**前**に`save()`を呼んでおり、`save()`が
   毎回`S.lastSeen = Date.now()`で上書きするため、復帰判定の「離脱時間」が常にほぼ0になり、
   「おかえりなさい」通知が**絶対に表示されない**状態だった。
2. さらに、タイトル画面の`startBtn`クリックハンドラ自身も`bootWorld()`を呼ぶ**前**に
   `save()`を呼んでおり（性別選択の保存のため）、①を`bootWorld`内で対症療法的に直しても
   この層でも同じ理由でlastSeenが上書きされ、実際のプレイ導線（タイトル→スタート）では
   依然としてバグが再現することを実ブラウザテストで確認した。

### 修正内容
`load()`直後（まだ一度も`save()`していない時点）の`S.lastSeen`を`init()`のトップレベルで
`const prevLastSeen = S.lastSeen;`として確保し、`startBtn`クリック→`bootWorld(prevLastSeen)`→
`showWelcomeBack(prevLastSeen)`まで一貫して引き回す形に修正。これにより`save()`が何度呼ばれても
本来の離脱前`lastSeen`が失われないようにした。

### 検証結果（localStorage直接編集による離脱シミュレーション→実際のstartBtnクリック）
1. `lastSeen`を5分前、畑0を収穫可能状態、`lastEggAt`を卵2個分過去に設定
2. ページ再読み込み→**実際の`startBtn`クリック**→600ms後に「🏡 おかえりなさい！
   🥕さくもつが3こそだったよ！🥚たまごがうまれてるよ！」が正しく表示（修正確認）
3. 閉じた直後に再度読み込み→再度`startBtn`クリック→今度は離脱時間が短い（<60秒）ため
   通知が再表示されない（同じ復帰報酬を二重通知しないことを確認）
4. 卵の`EGG_MAX=3`キャップ（§2で確認済み）により過剰報酬にはならない

**判定：合格（発見した重大バグ2件は修正済み）**

## 10. 初期化・残留確認（実ブラウザ確認）

1. もちもの/ショップ/キッチンモーダルの開閉を計30回連続実行 → 例外なし、`modalOpen`最終的にfalse
2. `ensureAudio()`/`initBgm()`を10回連続呼び出し → `AudioContext`は1個のみ生成（ガード確認済み）、
   `_bgm`も1個のみ（`if(_bgm) return;`ガード確認済み）
3. `load()`→`bootWorld()`を10回連続実行 → 例外なし、`friendHouseGroup`に家メッシュの重複追加なし
   （`builtFriendHouses`ガードが有効）
4. 料理の開始→中断を10回連続 → `mini`残留なし（§3参照）
5. `tick()`のrequestAnimationFrameループは`init()`内で1回のみ起動される設計であり、
   `bootWorld()`や`load()`の再実行では新たなループを生成しないことをコード確認済み

**判定：合格**

## 11. 一時デバッグ削除確認

本Phaseの検証では、待ち時間短縮等の目的で**ファイルに一切の一時デバッグコードを追加していない**
（すべてブラウザの実行中コンテキストに対するJS実行による状態操作・関数呼び出しのみで検証したため、
削除すべき残留物自体が存在しない）。

```
$ grep -n "debugMuraPhase1" public/games/mura_v3.html    → 該当なし（exit 1）
$ grep -n "TEMP DEBUG" public/games/mura_v3.html          → 該当なし（exit 1）
$ grep -niE "forcespawn|forcemove|forcemission" public/games/mura_v3.html → 該当なし（exit 1）
```

## 12. 最終検証

- `node --check`（インラインscript抽出）：OK
- `npm run build`：成功（client + SSR + 全ルートprerender、`/mura`含む）
- `npm run audit:games`：PASSED（`mura_v3.html`について新規の警告は
  `[HTML] id "mClose" が6回定義されています`のみ。全モーダルが`openModal()`による
  innerHTML全置換方式のため実行時に共存せず、実害なし。30回の開閉ストレステストでも
  例外・残留は確認されなかった）
- `package-lock.json`：差分なし
- `reports/game-audit.json`：ビルド後に差分が出たため`git checkout --`で復元済み
- `src/games/Mura.jsx` / `src/seo/gameMeta.js` / `src/pages/TopPage.jsx`：無変更
- `public/games/mura_v1.html` / `public/games/mura_v2.html`：無変更

## 13. 未確認事項

- §8の追加検証により、着せ替え全8カテゴリ（face/shoes/fxを含む）の3D反映・歩行追従・
  再読み込み後の維持を実ブラウザで目視確認済み。未確認事項なし。
- 森・川上・海岸などPhase 2以降の機能は今回スコープ外（意図的）

## 14. 使用した一時デバッグ

なし（§11参照）。ファイルへの追加は一切行っていない。§8のfx/shoes検証で用いた
「カメラの自動追従を撮影直前だけ一時的に無効化する」操作は、実行中のJSインスタンスに
対する一時的なメソッド上書き（`camera.position.lerp`/`camera.lookAt`をno-op化し、
撮影後に`delete`で復元）であり、ゲームファイル・セーブデータには一切残らない。

## 15. 視認性調整（ユーザー指摘によるpush前の修正）

Phase 1の機能面レビュー後、実プレイ画面全体が白っぽく、草地・道・建物・キャラクターの
コントラストが弱いとの指摘を受け、以下を修正した。

### 原因

- `renderer.toneMapping`が未設定（既定の`NoToneMapping`）だったため、
  `HemisphereLight(0.9)` + `DirectionalLight(1.0)`の合計光量が単純加算され、
  淡いパステル系マテリアル色が白側へクリップしやすい状態だった。
- `HemisphereLight`（全方向からの塗りつぶし光）が0.9とやや強く、
  `DirectionalLight`（主光源・影の作成者）に対して相対的に強すぎたため、
  影のコントラストが弱まっていた。
- 小道の色（`0xe8d9a8`）が草地の色（`0x9fdc7e`/`0x7cc95f`/`0xb8e698`）と
  明度・彩度の両面で近く、上記の照明状態と組み合わさって境界がほぼ消えていた。

### 変更内容

- `renderer.toneMapping = THREE.ACESFilmicToneMapping;`
  `renderer.toneMappingExposure = 1.0;` を追加（r128対応のAPIのみ使用、
  新しい`outputColorSpace`は使用していない）
- `HemisphereLight`の昼間強度を`0.9`→`0.62`に低減。`DirectionalLight`（`sun`）は
  `1.0`のまま変更なし（主光源として維持）
- `updateSky()`内の夕方・夜・朝焼け各フェーズのhemi強度も、昼の基準値変更に
  比例させて再計算し、時間帯間の連続性を維持（`0.9→0.62`, `0.55→0.38`, `0.35→0.24`）
- 小道の色を`0xe8d9a8`→`0xb89a68`に変更（草地色との差を明確化。草地・木・住民・
  キャラクター本体の色は変更していない）

### 検証結果（実ブラウザ、GPU実ピクセル読み取りによる数値確認）

`gl.readPixels`で実際のレンダリング結果を直接読み取り、変更前後を比較した
（プレイヤー位置(0,2)・カメラ標準追従・同一dayPhase(≒0.04、昼)で比較）。

- 道と草地の明度差：変更前は同一アングルで判別が困難なレベルの近さだったのに対し、
  変更後は道(198,193,173)・草地(188,209,179)と、明度・色相（草地は緑チャンネルが
  明確に高い）の両面で区別可能な差になった
- 修正前ファイル（commit時点のもの）と修正後ファイルを同一dayPhaseで並べて
  スクリーンショット比較し、境界の視認性向上を目視確認
- 夜間（`dayPhase`を0.78へ一時的に強制して確認）：`hemi=0.24, sun=0.15`、
  `renderer`のclearColorが`#2b3967`（暗い紺）になることを確認。
  `gl.readPixels`による実測でも草地ピクセルが(96,129,94)まで暗化することを確認した
  （※ブラウザ操作用スクリーンショットツール側に数秒のキャッシュ遅延があり、
  スクリーンショット画像自体は即座には反映されなかったが、GPU描画バッファの
  実測値では正しく暗化していることを確認済み）
- 夕方（`dayPhase=0.6`）：`hemi=0.50, sun=0.70`と、昼と夜の中間の値に正しく遷移
- UI要素（HUD・ボタン・モーダル）はThree.jsのレンダリング対象外のDOM要素であり、
  今回の変更で色・不透明度とも一切変化していないことをコードレベルで確認
  （変更箇所はrenderer/light/道マテリアルのみ）

### 最終検証
- `node --check`：OK
- `npm run build`：成功
- `package-lock.json` / `reports/game-audit.json`：差分なし
- `src/games/Mura.jsx` / `src/seo/gameMeta.js` / `src/pages/TopPage.jsx` /
  `public/games/mura_v1.html` / `public/games/mura_v2.html`：無変更
