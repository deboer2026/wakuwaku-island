# どうぶつのむら v3 Phase 4 テスト計画

## 実ブラウザ確認

- 村の3出口から森・川・海へそれぞれ遷移し、村への復帰位置・ペット・衣装・BGMを確認する。
- 森の採集物、川の採集物と釣り、海の漂着物を採集し、再出現・再読込後の状態を確認する。
- 各地域の宝箱を一度だけ取得し、再入場・再読込で再取得できないことを確認する。
- 川で釣りを10回連続実施し、成功・失敗・中断・地域退出後のUI残留がないことを確認する。
- 朝・昼・夕・夜に各地域を確認し、プレイヤー・ペット・採集物・出口が視認できることを確認する。
- 地域切替50回、3地域20往復、採集100回を行い、重複メッシュ、入力不良、エラー、複数RAFがないことを確認する。

## 保存・移行確認

- schemaVersion 5 と旧セーブから、既存の畑・料理・住民・室内・衣装を残したままschemaVersion 6へ移行できること。
- 地域位置、採集クールダウン、地域クエスト、宝箱、探索XPが保存されること。
- 無効な `currentArea` は村へ戻ること。

## 実施結果（2026-07-26）

- **実ブラウザ確認**：一時的な地域遷移UI（確認後に削除）から、村→森→村、村→川→村、村→海→村を確認した。各地域の地形・採集物が表示され、村復帰後もコンソール error は 0 件だった。
- **実ブラウザ確認**：森で4種、川で水辺の草・うろこ・魚、海で貝・流木・海藻を採集し、各地域の宝を一度取得した。魚は成功・失敗・中断を確認し、10回連続の開始／中断後も釣りモーダルは残らなかった。
- **ブラウザ実行コンテキスト調整による確認**：採集時刻を過去にして、森・川・海の各1ノードが未経過では再取得されず、経過後に再出現して再取得できることを確認した。
- **ブラウザ実行コンテキスト調整による確認**：Phase 3相当（schemaVersion 5、探索フィールドなし）の状態を移行し、`gathered`、`exploration`、探索XP／ランクを補完しつつ schemaVersion 6 へ更新した。不正な `currentArea` は village に戻り、通常URLでの再起動後も error は 0 件だった。
- **実ブラウザ確認**：地域切替を78回、森で採集を100回相当実施した。重複した地域UI・採集ノード・釣りタイマー、およびコンソール error は確認されなかった。
- **ブラウザ実行コンテキスト調整による確認**：森・川・海で昼と夜を比較し、森では朝・夕方も比較した。プレイヤー、採集物、宝・出口の目印を視認でき、夜の暗転や水面だけの過度な明るさはなかった。
- **ブラウザ実行コンテキスト調整による確認**：森（きのこ3個）、川（魚2匹）、海岸（貝3個）の地域依頼を各1件完了した。必要数未満では完了せず、完了時だけ素材を消費して、クラウン5・探索XP8・一回限りの完了モーダルを得た。通常注文・住民依頼とは別の `exploration.quests` に保存され、再読み込み後も3件の完了状態を維持した。
- **ブラウザ実行コンテキスト調整による確認**：川の地域依頼完了時、探索XPが31となり、HUD表示の探索ランクが R1 から R2 へ上昇した。再読み込み後は R2 / XP48 を維持した。
- **実ブラウザ確認**：村→森→村、村→川→村、村→海岸→村のテーマ状態を確認した。村は `happy`、森は `forest`、川は `adventure`、海岸は `ocean` で、いずれも `wakuwaku_bgm.js` の実在テーマである。地域切替後にコンソール error は0件だった。

一時検証UI、時間固定、テスト用保存操作はすべて削除済みである。BGMの聴感確認は自動化環境の出力制約により行わず、テーマ切替状態・例外なし・既存の単一BGMインスタンス設計を確認した。

## 静的確認

- 全インラインscriptの `node --check`
- `npm run build`、SSR、39ルートのプリレンダ
- `git diff --check`
- `package-lock.json`、`reports/game-audit.json`、保護対象ファイルに差分がないこと
- 一時デバッグ用識別子が0件であること
