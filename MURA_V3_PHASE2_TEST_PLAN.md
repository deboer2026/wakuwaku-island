# mura_v3.html Phase 2 実プレイ検証計画・結果

区分の凡例（Phase 1のテスト計画と同じ）：
- **実ブラウザ確認**：ビルド後のファイルを実際にロードし、UIボタンの実クリック
  （DOMイベント発火）で確認したもの。ゲーム側の実装は変更していない。
- **一時デバッグ経由の実ブラウザ確認**：待ち時間の長い条件だけを
  `?debugMuraPhase2=1` で有効になる確認UIから設定し、その後は実際のゲームUI・
  本番コードで確認したもの。一時実装は最終確認後に完全削除し、通常URLでは
  デバッグUIが存在しないことも実ブラウザで確認した。
- **未確認**：今回検証できなかった項目。

なお、今回のブラウザ自動操作環境では、タブがバックグラウンド扱いになると
`requestAnimationFrame` ループが一時停止する（実ユーザーがタブを開いて操作している
状況では発生しない、自動テスト環境固有の挙動）。この影響で `tick()` が進まず
アクションボタンの表示が更新されないことがあったため、検証中は `tick(performance.now())`
を明示的に呼んで1フレーム分だけ進める操作を併用した。これはテスト手法であり、
本番コードやゲームロジックには含まれない。

## 1. 重大バグの発見・修正（実装直後、実プレイ前に判明）

実装直後に室内へ入る操作を初めて実ブラウザで試したところ、**ゲームループ全体が
停止する**重大な不具合が2件連鎖して発生していた。コード確認だけでは気づけず、
実際に入室操作を行って初めて発覚した。

1. **`scene.fog = null` によるクラッシュ**：室内では「近距離が白飛びしないように」
   という意図で `scene.fog` を一時的に `null` にしていたが、`updateSky()` が
   `scene.fog.color.copy(...)` を無条件に呼んでいたため、室内に入った瞬間から
   毎フレーム例外が発生し、`tick()` が `renderer.render()` に到達できず画面が
   一切更新されなくなっていた（退出ボタンを押しても描画されず、詰みの状態になる
   致命的なバグだった）。`if(scene.fog) scene.fog.color.copy(fogC);` にガードを
   追加して修正。そもそも村のfogは`near:40`のため室内の近距離オブジェクトには
   最初から影響しておらず、fogをnullにする処理自体が不要だったことも判明した。
2. **村を隠す処理がグローバル照明まで非表示にしていた**：室内進入時に
   「村の全オブジェクトを非表示にする」処理が、`scene`に直接addされている
   `HemisphereLight`/`DirectionalLight`（村・室内共通の照明）まで`visible=false`に
   しており、室内でキャラクターが完全な黒いシルエットになっていた。
   `if(o instanceof THREE.Light) return;` を追加し、照明は非表示対象から除外した。
3. **室内カメラが壁の外に出る**：室内の追従カメラが部屋の奥行きを越え、入口付近で
   壁外（何も描画されない空間）へ出ていた。最初のクランプ修正では壁外は防げたが、
   入口でカメラがプレイヤーまで約0.2mへ接近し、キャラクターが画面を占有する
   二次不具合が最終確認で判明した。
4. **入口クランプによるカメラ接近**：室内だけを共通の上方固定ドールハウス視点へ
   切り替え、`INTERIOR_CAMERA` にFOV・希望位置・追従ターゲット・最低距離4.4を集約。
   希望位置算出→最低距離補正→`lookAt`の順に統一し、室内ごとの場当たり座標、
   プレイヤー位置変更、追従カメラとの毎フレーム競合をなくした。

これら3件を修正した後、実際に室内が正しく表示・照明されることを確認した
（本ドキュメント§2以降の実プレイ確認はすべて修正後のもの）。

## 2. プレイヤー宅（実ブラウザ確認）

1. 村でプレイヤー宅のドアに近づく →「🚪 おうちに はいる」実表示・実クリック
2. `S.currentArea` が `"interior:player"` に変化、入口付近（部屋内）へ配置
3. スクリーンショットで床・ラグ・ベッド・テーブル・すがたみが正しく表示され、
   キャラクターも適切に照らされていることを確認
4. すがたみに近づく →「🪞 きせかえを みる」実クリック → Phase1と同一のワードローブ
   モーダル（`openようふくやさん`）が開くことを確認（重複実装なし）
5. 「🚪 そとに でる」実クリック → `S.currentArea` が `"village"` に戻り、
   ドア前（村座標）に配置。スクリーンショットで村全体（木・家・道・視認性調整）が
   完全に復元されていることを確認
6. 入室直後・部屋中央・壁際・退出直前の4位置で、カメラが近づきすぎず、壁を抜けず、
   ベッド・テーブル・出口が同時に判別できることを実ブラウザで確認

**判定：合格**

## 3. キッチン（実ブラウザ確認）

1. キッチンのドアへ近づき「🍳 キッチンに はいる」実クリックで入室
2. 材料（にんじん3・トマト3）を用意し、調理台へ近づいて「🍳 りょうりする」実クリック
   → Phase1と同一のキッチンモーダルが開く
3. やさいサラダを選択 → 切る（3タップ、`cutTap`実クリック）→焼く（ゾーン内で
   `panStop`実クリック）→ ★3で完成、`S.dishes` に正しく追加されることを確認
4. 完成演出（`spawnBurst`）が室内の食器テーブル位置で発生し、`bursts`配列が
   時間経過で0に戻る（残留なし）ことを確認
5. 退出して村へ戻ることを確認
6. 入室直後・部屋中央・壁際（調理台前）・退出直前で、共通室内カメラが料理台・棚・
   出口を保ったまま表示することを実ブラウザで確認（料理工程自体は既確認のため再実施なし）

**判定：合格**

## 4. 服屋（実ブラウザ確認）

1. 服屋のドアへ近づき「🛍️ ふくやに はいる」実クリックで入室
2. ラック（衣装掛け）へ近づき「🛍️ おかいもの」実クリック → Phase1と同一の
   ワードローブモーダルが開くことを確認
3. head枠でベレー帽を購入・装着 → `S.wardrobe.head` が更新され、スクリーンショットで
   ラック・カウンター・店員らしき人型オブジェクトと、装着後のキャラクター
   （赤いベレー帽）を確認
4. 退出して村へ戻ることを確認

**判定：合格**

## 5. 既存住民5軒（実ブラウザ確認）

`S.friends[id]=5` を設定→`rebuildFriendHouses()`で家を村に出現させた上で、
ねこ・うさぎ・くま・きつね・ぱんだの5軒すべてについて、ドア出現→実クリック入室→
`S.currentArea` が対応する `interior:resident_<id>` に変化することを確認した。

**判定：合格**

## 6. 新規住民3体・3軒（実ブラウザ確認）

りす・ふくろう・かわうそについても、既存住民と同じ移住条件（仲良し度5）で
家が村に出現し、ドア実クリックで入室できることを確認。3体とも3D形状
（耳・しっぽの形）が既存5体と異なる専用ジオメトリで表示されることをコード
生成ロジック（`buildAnimal`の分岐）と実際の室内スクリーンショットの両面で確認した。

**判定：合格**

## 7. 8住民会話（実ブラウザ確認）

8住民すべての家へ順番に入室し `talkAnimal(id)` を実行、表示された台詞が
以下の通りそれぞれ異なることを確認した（使い回しなし）。

| 住民 | 表示された台詞（今回の状態に応じたもの） |
|---|---|
| neko | 「ミルクの いちばんの ともだちだね！」（仲良し最大） |
| usagi | 「やっほー！モモだよ！なかよくしようね！」（初対面） |
| kuma | 「おう、ボルトだ。よろしくな。」（初対面） |
| kitsune | 「やあ、コンだよ。ふふ、なにか おもしろい こと ない？」（初対面） |
| panda | 「サラダを いただけますか？」（注文中） |
| risu | 「あ、あの…ぺかんです。よろしく…おねがいします。」（初対面） |
| fukurou | 「ホーホー、ノノです。よるは とくいなんですよ。」（初対面） |
| kawauso | 「サラダ たべたいなあ」（注文中） |

**判定：合格**

## 8. 個別依頼（実ブラウザ確認、3種類のタイプを確認）

1. **inv型（ねこ：とうもろこし3個）**：材料不足時は「わたす」ボタンが出ず
   依頼テキストのみ表示 → `S.inv.corn=3` にして再度話しかけると「わたす」ボタン出現
   → 実クリックで達成、`S.inv.corn`が0に減算、クラウン+20、questDone=trueを確認
2. **find型（ふくろう：ほんだなを調べる）**：室内のほんだなオブジェクトに近づき
   「📚 ほんだなを しらべる」実クリックで即達成、クラウン加算を確認
3. **well型（かわうそ：井戸でみんなに水をあげる）**：村の井戸で「⛲ みんなに みず」
   実クリック（`useWell()`）により、村にいながらにして達成されることを確認

いずれも `S.residents[id].questDone` により二重達成・二重報酬にならないことを、
既に達成済みの依頼を再度実行しようとしても状態・クラウンが変化しないことで確認した。

**判定：合格（最低2件の指示に対し、3件・3タイプを確認）**

## 9. 仲良しイベント2件（実ブラウザ確認）

ねこの家で、個別依頼クリア後（仲良し度5・questDone=true）に退出→再入室すると
「💛 なかよしイベント！」モーダルが自宅専用台詞とともに初回のみ表示されることを確認。
続けてもう一度退出→再入室しても再表示されない（`eventSeen`フラグによる一度きり管理）
ことも確認した。

2件目として、うさぎの仲良し度5・依頼完了・未閲覧を一時デバッグ条件で作成して
うさぎ宅へ入室した。ねこと異なる固有台詞「おきにいりの リボンが かざってあるの！」
が表示され、依頼結果の「リボンの かざり」を示すギフト棚も残った。退出→再入室、
ページ再読み込み、さらに10回の入退室後もイベントは再発火しなかった。

残り6住民はコード確認により、`homeOnly[1]` と `giftLabel` が全員で固有であり、
共通の `eventSeen` 判定・保存経路を使用することを確認した。

**判定：合格（ねこ・うさぎの2件を実ブラウザ確認、残り6件はコード確認）**

## 10. 入退室（実ブラウザ確認）

プレイヤー宅・キッチン・服屋・住民8軒すべてについて、ドア実クリックでの入室と
「そとに でる」実クリックでの退出を確認。退出後は必ず対応する建物のドア前
（村座標）に配置され、村の表示（地形・木・他の建物・視認性）が完全に復元される
ことをスクリーンショットで確認した。

**判定：合格**

## 11. セーブschema更新・Phase 1セーブ互換（実ブラウザ確認）

1. 住民の家の中でセーブ →ページ再読み込み → `S.currentArea` が同じ室内エリアIDの
   まま復元され、入口付近に配置されることを確認（スクリーンショットでkuma宅の
   室内が再現されていることを確認）
2. `residents`/`buildings`/`currentArea` キーを持たない Phase 1形式の
   `mura_v3_progress` を `localStorage` に直接投入して読み込ませ、以下を確認：
   - crowns/level/inv/dishes/komugiUnlocked/wardrobe が完全維持
   - 既存5住民の仲良し度（neko:5, usagi:2, kuma:0, kitsune:0, panda:0）が維持
   - 新規3住民（risu/fukurou/kawauso）の仲良し度が0で安全に補完
   - `residents`/`buildings`/`currentArea` が欠損なく既定値で補完（`village`起動）
   - `schemaVersion` が4に更新
3. `S.residents.usagi.questDone = true` にしてセーブ→再読み込みしても、
   再移行によって上書きされず維持される（冪等性）ことを確認

**判定：合格**

## 12. 着せ替え室内反映・ペット室内表示（実ブラウザ確認）

- 服屋室内で装着した頭装備（ベレー帽）が、そのままキャラクターの3Dモデルに
  反映されていることをスクリーンショットで確認（`rebuildPlayerLook()`は場所を
  問わず同一処理のため、室内特有の分岐は存在しない）
- 通常の着せ替えUIから「ちょうちょ」を購入済み一覧より装着し、村・プレイヤー宅・
  キッチン・うさぎ宅のすべてで1体だけ表示されることを実ブラウザで確認した。
- 室内移動でプレイヤーへ滑らかに追従し、壁・床・家具への大きな埋没がなく、
  退出後も村で追従を継続した。
- 同じうさぎ宅へ10回入退室しても1体のまま増殖せず、ページ再読み込み後も装備状態と
  室内表示が維持された。村用と室内用の別状態はなく、Phase 1の `S.wardrobe.pet` と
  同一の `petMesh` を共有している。

**判定：合格（着せ替え・ペットとも実ブラウザ確認）**

## 13. 50回入退室・多重化・残留確認（実ブラウザ確認）

1. 8住民の家を順番に、合計50回入室→退出を繰り返した
2. `interiorGroups` のキー数が11のまま（3建物＋8住民、想定通り）で、繰り返しによる
   重複生成がないことを確認
3. 村オブジェクトの表示状態がすべて正しく復元され（`hiddenNonInteriorCount:0`）、
   `savedVillageVisibility` が正しくnullに戻っていることを確認
4. `hemi.visible`/`sun.visible` が常にtrueのまま（村非表示処理の影響を受けない）
5. `AudioContext`/`WakuwakuBGM` を10回連続初期化しても1個のみ生成されることを確認
   （Phase1と同じガードのため、Phase2の変更による影響なし）
6. 料理の演出パーティクル（`bursts`）が時間経過で確実に0へ戻ることを確認
7. ペット装備後に同じ住民宅へ10回入退室し、ペットが1体のままで、室内の明るさも
   変化しないことを画面比較で確認。室内グループは初回構築後にキャッシュされるため、
   PointLightも入退室ごとに追加されないことをコードでも確認

**判定：合格**

## 14. コンソールエラー（実ブラウザ確認）

上記すべての操作を通じて、ブラウザコンソールにエラーが出力されていないことを
`read_console_messages({onlyErrors:true})` で確認した（§1で発見した3件のバグは
いずれも修正後の状態で最終確認している）。

**判定：合格**

## 15. 一時デバッグ削除確認

最終確認では、ブラウザ安全制約下で仲良し条件と時間帯を切り替えるため、
`?debugMuraPhase2=1` の場合だけ有効になる一時UIをファイルへ追加した。確認後に
実装を完全削除し、通常URLの実ブラウザ起動でデバッグUIが0件であることを確認した。

```
$ grep -n "debugMuraPhase2" public/games/mura_v3.html          → 該当なし（exit 1）
$ grep -n "TEMP DEBUG" public/games/mura_v3.html                → 該当なし（exit 1）
$ grep -niE "forcemove|forcefriend|forcequest" public/games/mura_v3.html → 該当なし（exit 1）
```

## 16. 最終検証

- `node --check`（インラインscript抽出）：OK
- `npm run build`：成功（client + SSR + 全ルートprerender、`/mura`含む）
- `npm run audit:games`：PASSED。`mura_v3.html`の新規警告は
  `id "mClose" が5回定義されている`のみ（Phase1から続く既知の設計:
  各モーダルが`openModal()`によるinnerHTML全置換方式のため実行時に共存せず実害なし）
- `package-lock.json`：差分なし
- `reports/game-audit.json`：ビルド後に差分が出たため`git checkout --`で復元済み
- `src/games/MuraGame.jsx` / `src/seo/gameMeta.js` / `src/pages/TopPage.jsx` /
  `public/games/mura_v1.html` / `public/games/mura_v2.html`：無変更

## 17. 未確認事項

- 仲良しイベントは8住民のうち2件（ねこ・うさぎ）を実ブラウザで詳細確認。残り6件は
  固有データと共通の発火・保存ロジックのコード確認に留まる
- 森・川上・海岸などPhase 3以降の機能は今回スコープ外（意図的）
- 家具編集UI（Phase 2ではデータ構造のみ将来対応、UI自体は未実装のため対象外）

## 18. 使用した一時デバッグ

仲良し条件・エリア・時間帯だけを切り替える `?debugMuraPhase2=1` 用UIを一時使用し、
検証後に完全削除した（§15参照）。§1で発見した不具合はいずれも一時的な回避ではなく、
恒久修正としてファイルに反映済み。

## 19. 時間帯別の室内照明（実ブラウザ確認）

同じうさぎ宅・同じカメラ位置で朝（0.95）・昼（0.25）・夕（0.60）・夜（0.75）を
切り替え、4枚のスクリーンショットを比較した。全時間帯でプレイヤー・住民・ペット・
床・壁・テーブル・ベッド・ギフト棚・出口表示を判別でき、昼の白飛びと夜の暗転はない。
室内では屋外のsun/hemiを低い固定強度にし、各室内の暖色PointLightを補助光として
維持する。夜の室内から村へ退出すると屋外の夜間表現へ正常に戻り、再入室後も
明るさは増加しなかった。

**判定：合格（4時間帯すべて実ブラウザ確認、ライト非増殖は実ブラウザ＋コード確認）**
